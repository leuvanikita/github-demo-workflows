name: Demo workflow

on:
  push:
    branches: [main]

jobs:

  code:
    runs-on: ubuntu-latest
    steps:
      - name:  Checkout code
        step: echo "Code clone"

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Docker build
        step: echo "docker build"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test code
        step: echo "Test code"

  deploy:     
    runs-on: ubuntu-latest
    steps:
      - name: Deploy code
        step: echo "docker compose up or down"